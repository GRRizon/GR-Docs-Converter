# GR-Docs-Converter
A web app that converts between various formats (PDF, DOCX, PNG, JPG, WebP, Markdown) entirely within the browser using JavaScript libraries. This ensures 100% privacy and high speed since no data is uploaded to a server.
